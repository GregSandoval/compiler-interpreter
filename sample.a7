prog

main {
    var(
        int value = 99;
        int valuePtr = &value;
        int valuePtrPtr = &valuePtr;
        int current = 0;
    )

    print("valuePtrPtr => ");

    current = valuePtrPtr;
    print(current);

    current = *current;
    print(" => ", current);

    current = *current;
    print(" => ", current);
}
